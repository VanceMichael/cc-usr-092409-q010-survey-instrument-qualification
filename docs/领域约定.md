# 领域约定

原始采集包不可覆盖；构件对应关系随基线版本发布；隔离数据只有在摘要、序号和坐标信息完整后才能参与仲裁。

## 仪器资格与借用链

- 观测导入即冻结设备、证书与租约版本；原始采集包不可覆盖、不可删除。
- 重叠租约在同一事务内检查并拒绝；出借须保管人确认，归还须领用人确认。
- 合格观测进入候选解；过期、越区或组件不匹配的观测进入待复核，由复核裁定为采用、降级或隔离。
- 证书撤销与迟到的维修结论只重评尚未裁定的观测；已签署基线保留原资格快照并追加风险。
- 离线交接封签唯一，并发确认只有一次成功；停服恢复后继续逾期归还、证书到期与复核任务。
